name: Hexo Publish
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install Node.js 12 and Hexo
        uses: actions/setup-node@v2-beta
        with:
          node-version: '12'
          check-latest: true
      - run: touch /home/runner/work/tmp
      - run: cd /home/runner/work/tmp
      - run: pwd
      - run: npm install hexo-cli -g
      - run: hexo version
      - run: hexo init
      - name: 上传结果
        uses: actions/upload-artifact@v2.2.2
        with:
            # Artifact name
            name: hexo
            # A file, directory or wildcard pattern that describes what to upload
            path: /home/runner/work/tmp
